# Guitar Chord Lab — 設計の憲法

指板を押さえるとコード名の候補が出て、コード名を入れると押さえ方が並ぶ。
その2つをタブで行き来する、中〜上級者向けのコードツール。

この文書は**新しく決めた規則ではなく、`index.html` に既に実装されている規則を
抽出したもの**である。コード中の `section 5-4` `spec 4-1` といった参照は、
リポジトリに残っていない元の仕様書を指す。この文書がその代わりを務める。

規則と現状が食い違っていたら、**まず食い違いを報告すること。**
黙ってどちらかに寄せない。

`README.md` は外向きの説明。設計原則の記述は重なるが、
変更を加えるときに従うのはこの文書。

---

## 1. 動かしてはいけない制約

これらは機能ではなく前提。破る変更は提案の前に必ず確認を取る。

- **依存関係ゼロ。`index.html` 1ファイル。** ビルドツール、パッケージマネージャ、
  バックエンド、CDN、フォント読み込み、いずれも無い
- **外部リクエスト0件。** 現在ファイル内にある URL は SVG 名前空間
  (`http://www.w3.org/2000/svg`) だけで、これはリクエストではない
- **`file://` で動く。** ダウンロードしてダブルクリックで開ける。
  `history.replaceState` が `file://` で失敗するブラウザがあるため
  `saveURL()` は try/catch で囲んである。同じ配慮を怠らない
- **埋め込み（iframe / Artifact）でも動く。** ファイル末尾に viewport meta を
  自前で挿入する処理がある。head を前提にしない
- 音源ファイルを持たない。音は Web Audio + Karplus-Strong で合成する

## 2. 主役と階層

機能は**タブで分ける**。2つの機能は対等に扱う。

| タブ | 入口 | 出力 |
|---|---|---|
| 指板から調べる | 指板をタップ | 判定結果 |
| コード名から探す | コード名の入力欄 | 押さえ方のカード |

**両方のタブのラベルは常に見えていること。**
片方が「そこにあると分からない」状態を作らないための規則。
過去に検索側が1画面ぶん下（デスクトップ 949px / モバイル 1166px）にあり、
実質存在しない機能になっていた。タブは中身が隠れていても入口は見えている。
タブ自体も画面上部に固定する（`position:sticky`）。行き来にスクロールを
要求した時点で、分けた意味が失われる。

**指板は「指板から調べる」タブにのみ存在し、そこでは主役。**
「今の状態」を表す唯一の場所という位置づけは変わらない。
判定結果は指板の直接の出力なので、指板の直下から動かさない。

チューニング・カポ・表記は楽器そのものの設定で、押さえ方の生成も
これに依存するため**両タブで操作できる**。度数の色の凡例も、
両タブが色の付いた点を描くので両方に要る。

**両タブで要るものは複製せず、1つのノードをアクティブなタブへ移動させる**
（`#instRow`、`#legend`）。`setTab()` がスロット（`.instSlot` / `.legendSlot`）
へ付け替える。重複IDも同期ズレも構造的に起こらない。

### なぜタブにしたか（2026-09、旧規則からの変更）

以前は2つの機能を1本のスクロールに縦積みしていて、「モードを作らない」
「タブで分割しない」が規則だった。実使用で2つの問題が出た。

1. 検索語と指板が独立しているため、指板をいじると **D11 の判定結果の
   すぐ下に C の押さえ方が20枚並ぶ**。同時に見えることが害になっていた
2. 機能を足すたびに縦に伸びる構造は、3つ目以降で確実に破綻する。
   「この2つだけが隣接している」構造は拡張の足かせになる

縦積みの利点だった往復（判定 → 候補タップ → カード → タップ → 指板）は、
**タブの自動切り替えと状態保持**で置き換えた。§4 を参照。

## 3. 視覚の憲法

### 色

**すべて `:root` のトークン経由。生の色を新しく書かない。**

- 地: `--bg` `--surface` `--line` `--line-soft`
- 文字: `--tx-1` 〜 `--tx-4` の**4段階のみ**。純黒は使わない。
  `--tx-4` は `#9A9A9A` から `#6B6B6B` に引き上げてある
  （off-white 地で 2.7:1 → 5.1:1。WCAG AA を満たすため）。**戻さない**
- アクセントは `--accent` **1色のみ**。Root の度数色と同じ色を共有し、
  ページ全体が1つの色相で収まるようにしてある
- **度数の色（`--c-root` 〜 `--c-none`）はデータの符号化専用。
  UIの装飾に絶対に使わない。** 色は常に「度数」を意味する。
  どの画面でもルートは同じ色で、押さえ方が変わっても構造が読める

複数の押さえ方を色分けで重ねる方式は3つ以上で破綻するため、採用しない。

### 文字

- フォントは `--font`（Noto Sans JP 系のスタック）のみ。追加しない
- 太さは CSS では **400 と 500 だけ**。700 は SVG 内のデータ表示にのみ使う
  （鳴っている開放弦の音名、カード内の指番号）。UIの装飾には使わない
- サイズは役割に紐づいている。新しいサイズを発明しない:

| px | 役割 |
|---|---|
| 22 / 20 | 判定第1位のコード名、`#mini` のコード名 |
| 17 | `h1` |
| 15 | タブ（機能の見出し）、コード名入力欄、判定結果のコード名 |
| 14 | 本文、サジェスト |
| 13 | フォーム部品、ボタン、`.lede` |
| 12 | セクション見出し、補助テキスト、件数、エラー |
| 11 | フィールドラベル、チューニング表示 |
| 10 | SVG内のラベル、バッジ |

### 余白

**4px 刻みのスケール `--s1:4px` 〜 `--s8:72px` を使う。**
現在40箇所で使われている。生の px を新しく足さない
（例外は 2〜12px の微調整で、既存箇所に限る）。

### 角丸・影・動き

- 角丸は `--r`（6px）。円は `50%`、ピルは `999px`。それ以外を発明しない
- **影は使わない。浮いている要素も境界線で表現する。**
  `#sug`（サジェスト）も `#mini`（下部バー）も `.tunepop`（ナットの
  ポップオーバー）も `border` のみ。`box-shadow` はファイル内に1つも無い
- トランジションは `.12s ease` のみ。`prefers-reduced-motion:reduce` で
  全て無効化される。スクロールも同様に尊重する（`scrollToEl()` を使う）

## 4. 相互作用の規則

- **タブの中にモードを作らない。** 機能の分割はタブの粒度まで。
  1つのタブの中で表示モードを切り替えるような分岐は作らない。
  指板は「指板から調べる」タブにひとつだけ存在する
- **タブをまたぐ操作は、自動でタブを切り替える。** 判定結果の候補タップは
  検索タブへ、カードタップは指板タブへ（`setTab()`）。押した結果が
  見えないタブで起きたままにしない
- **タブを離れても状態を捨てない。** 検索語・フィルタ・運指・スクロール位置
  （`scrollMemo`）はそのまま残す。**行き来が安いことがタブ分割の前提**であり、
  ここが崩れると分割そのものが成立しない
- **同じ情報を2箇所に置かない。** 置くなら、操作できるのも同じ場所にする。
  チューニングの音名がナット脇とパネルの両方にあり、操作できるのは
  パネルだけだった状態は、この規則の違反だった
- **新機能を「新しい箱」として足さない。** 既存の構造に畳み込めないか先に考える。
  弦別チューニングは専用パネルではなくナットの音名そのものに統合した
- **押した結果が画面外で起きるなら、そこへ連れて行くか、その場に手応えを出す。**
  判定結果の候補タップは `scrollToEl($("findSec"))` でカードまで移動する。
  入力欄の隣の件数（`597件 ↓`）はその場の手応え兼、移動手段
- タップ標的は 44px 以上を目安にする。指板上の升目だけは例外で、
  弦間隔（横32px / 縦52px）がそのまま granularity になる
- キーボードで到達・操作できること。SVG の要素を操作対象にする場合は
  `role="button"` `tabindex="0"` と Enter/Space を自前で配線する
  （`.tunehit` が実例）。開閉する要素には `aria-expanded`

## 5. 状態とデータの流れ

- 状態は `state` オブジェクト**1つだけ**。DOM から状態を読み戻さない
- 表示中のタブも状態（`state.tab`）。切り替えは `setTab()` 経由で、
  パネルの表示・タブの選択状態・`#instRow` の移動・スクロール復元・
  URL 保存をまとめて行う。個別に `hidden` を触らない
- 描画は `refresh()` = `renderBoard()` → `renderResults()` → `renderCards()` → `saveURL()`。
  状態を変えたら `refresh()` を呼ぶ。個別に DOM を書き換えて済ませない
- `state.fingering` は**絶対フレット**（`null` はミュート、`state.capo` が開放）
- 指板の座標は `geom()` が返す。縦横で値が変わるので、
  座標を関数の外にハードコードしない。`isVertical()` が向きを決める
- URL 状態は `saveURL()` / `loadURL()`。`URLSearchParams` を使わず手で組むのは
  カンマをそのまま残すため。**この意図を壊さない**
- 押さえ方の生成は約75万通りの総当たりで、**実測0.5秒程度かかる**（Cで597件）。
  `vCacheKey` でキャッシュし、`searchToken` で古い結果を捨てる。
  起動時の生成は `requestIdleCallback` で初回描画の後ろに送る
- イベントは `document` 1箇所への委譲 + `closest()` の分岐。
  指板は毎回 `innerHTML` で作り直されるため、要素に状態を持たせない

## 6. 音楽ドメインの規則

**`CHORD_TYPES` のスコアリング重みは、UIが存在する前に元仕様の全テストケースで
検証されている。再検証なしに触らない。**

- コードの同定は一意に決まらない。`C-Eb-G-Bb` は `Cm7` でも `Eb6` でもある。
  **答えを1つに絞らず**、12ルート × 34タイプ = 408通りをスコア順に並べる
- 省略が許されるのは各タイプの `omitOk` に定義された度数だけ
  （主に5度、11th/13th系の9th）。7th・6th・テンション・sus が
  鳴っていない候補は棄却する。これをやらないと候補がノイズだらけになる
- 順位だけでなく**理由**を併記する（「完全一致」「Eが構成音外」「ベースがGのため減点」）
- 生成した押さえ方は物理的に押さえられるものだけに絞る:
  フレット幅4以内（開放弦は幅に含めない）、セーハ判定、指4本以内、
  セーハ範囲の内側に開放弦がある形は除外
- カポがある時は実音とシェイプ名を併記する（`Eb` / `カポ3F: C`）

## 7. 何をしないか

- タブの中をさらにモードで分割しない（機能の分割はタブの粒度まで）
- 装飾のための色・影・グラデーション・絵文字を足さない
- ラベルで済むところに説明文を置かない
- 押さえ方のデータベースを持たない（その場で生成する）
- 依存関係を増やさない

## 8. 変更を加える前のチェックリスト

1. これは**既存の構造に畳み込めない**か。新しい箱を作ろうとしていないか
2. 同じ情報を2箇所に置いていないか
3. 使った色・サイズ・余白は**既存のトークン**か。新しい数字を発明していないか
4. **空・1件・大量・エラー・読み込み中**の状態を全部設計したか
   （検索側の「空」が設計されていなかったのが過去の失敗）
5. 押した結果が画面外で起きていないか
6. キーボードで到達・操作できるか。タップ標的は足りているか
7. 縦（モバイル）と横（デスクトップ）の両方で確認したか。
   タブを切り替えて戻ったとき、状態とスクロール位置が残っているか
8. `file://` で直接開いても動くか

## 9. 確認のしかた

このリポジトリにはテストランナーが無い。変更は**実際にブラウザで動かして**確かめる。
Playwright + Chromium が使える環境なら、`file:///…/index.html` を開いて
操作し、`pageerror` と `console` のエラーが0件であることまで見る。
指板やポップオーバーの当たり判定を変えた時は、`elementFromPoint` で
「押したつもりのものが実際に押される」ことを座標レベルで確認する。
